name: Publish
on:
  push:
    # tags:
    #   - '*'
    branches:
      - github_release

permissions:
  contents: write

env:
  version: v0.0.2-alpha.1

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos:
          - linux
          - darwin
          - windows
        goarch:
          - amd64
          - arm64

    steps:
      - uses: actions/checkout@v4
      - run: GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o main-${{matrix.goos}}-${{matrix.goarch}}-${{env.version}}
      - uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: main-${{matrix.goos}}-${{matrix.goarch}}-${{env.version}}
          # tag: ${{ github.ref }}
          tag: ${{env.version}}
          overwrite: true