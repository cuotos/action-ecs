name: Publish
on:
  push:
    # tags:
    #   - '*'
    branches:
      - github_release

permissions:
  contents: write

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os:
          - linux
          - darwin
          - windows
        arch:
          - amd64
          - arm64

    steps:
      - uses: actions/checkout@v4
      - run: GOOS=${{ matrix.os }} GOARCH=${{ matrix.arch }} go build -o main-${{matrix.os}}-${{matrix.arch}}-0.0.1
      - uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: main-${{matrix.os}}-${{matrix.arch}}-0.0.1
          # tag: ${{ github.ref }}
          tag: v0.0.1
          overwrite: true